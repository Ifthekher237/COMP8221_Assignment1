# Data Directory

Dataset files should not be manually stored here if they can be downloaded automatically through `torchvision`.

The dataset will be downloaded automatically later when the dataset loading code is implemented.

Large dataset files should not be included in the final submission if they can be downloaded automatically and reproducibly.
